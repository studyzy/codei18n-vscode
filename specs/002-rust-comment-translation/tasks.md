# 任务: Rust源码注释翻译支持

**输入**: 来自 `/specs/002-rust-comment-translation/` 的设计文档
**前置条件**: plan.md(已完成)、spec.md(已完成)、research.md(已完成)、data-model.md(已完成)、quickstart.md(已完成)

**测试**: 本功能规范要求单元测试覆盖率 > 80%,因此包含测试任务

**组织结构**: 任务按用户故事分组,以便每个故事能够独立实施和测试

## 格式: `[ID] [P?] [Story] 描述`
- **[P]**: 可以并行运行(不同文件,无依赖关系)
- **[Story]**: 此任务属于哪个用户故事(例如: US1、US2、US3)
- 在描述中包含确切的文件路径

## 路径约定
- **项目类型**: VSCode 扩展 (单一项目结构)
- **源码路径**: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/`
- **测试路径**: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/test/suite/`
- **根目录**: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/`

---

## 阶段 1: 设置(共享基础设施)

**目的**: 项目初始化和环境验证

- [ ] T002 [P] [Setup] 验证开发环境配置
  - 确认 Node.js >= 16.x: `node --version`
  - 确认 VSCode >= 1.74.0
  - 确认项目依赖已安装: `npm install`
  - 编译 TypeScript: `npm run compile`
  - 验证编译输出: `ls out/extension.js`

- [ ] T003 [P] [Setup] 检出功能分支并验证分支状态
  - 确认当前分支: `git branch --show-current` (应为 `002-rust-comment-translation`)
  - 验证无未提交更改: `git status`
  - 拉取最新代码: `git pull origin 002-rust-comment-translation`

**检查点**: 设置完成 - CLI 工具就绪,开发环境配置正确

---

## 阶段 2: 基础(阻塞前置条件)

**目的**: 在任何用户故事可以实施之前必须完成的核心配置和注释说明

**⚠️ 关键**: 在此阶段完成之前,无法开始任何用户故事工作

- [ ] T004 [Foundation] 更新 `package.json` 添加 Rust 激活事件
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/package.json`
  - 在 `activationEvents` 数组中添加: `"onLanguage:rust"`
  - 验证 JSON 语法: `npm run compile` 应无错误
  - **预期变更**: +1 行
  
- [ ] T005 [P] [Foundation] 为 `extension.ts` 添加 JSDoc 注释说明多语言支持逻辑
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/extension.ts`
  - 在 `activate()` 函数顶部添加 JSDoc 注释:
    ```typescript
    /**
     * Activates the CodeI18n VSCode extension.
     * Supports multiple languages (Go, Rust) for comment translation.
     * 
     * @param context - VSCode extension context for managing subscriptions
     */
    ```
  - 为 `supportedLanguages` 常量添加行内注释:
    ```typescript
    // Supported languages for comment translation (extensible list)
    const supportedLanguages = ['go', 'rust'];
    ```

**检查点**: 基础就绪 - 激活事件已配置,注释文档已补充

---

## 阶段 3: 用户故事 1 - Rust文件自动翻译注释(优先级: P1)🎯 MVP

**目标**: 实现 Rust 文件打开时自动翻译注释的核心功能,与 Go 语言保持一致的用户体验

**独立测试**: 打开任意包含英文注释的 Rust 文件,验证注释自动翻译为中文并正确显示

**验收标准**:
1. 打开 `.rs` 文件时插件自动激活
2. 行注释 (`//`)、块注释 (`/* */`)、文档注释 (`///`, `//!`) 都能翻译
3. 翻译在 3 秒内显示
4. 从 Go 文件切换到 Rust 文件时两种语言注释都正确显示

### 用户故事 1 的测试(TDD 顺序)

**注意: 先编写这些测试,确保在实施前它们失败**

- [ ] T006 [P] [US1] 在 `src/test/suite/rustSupport.test.ts` 中编写 Rust 文件激活测试
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/test/suite/rustSupport.test.ts` (新建)
  - 测试用例 1: 验证 Rust 文件的 `languageId` 为 'rust'
    ```typescript
    test('Rust file language ID recognition', async () => {
      const doc = await vscode.workspace.openTextDocument({
        language: 'rust',
        content: '// Test comment\nfn main() {}'
      });
      assert.strictEqual(doc.languageId, 'rust');
    });
    ```
  - 测试用例 2: 验证打开 `.rs` 文件触发激活事件
  - **预期变更**: 新增文件,约 20 行

- [ ] T007 [P] [US1] 在 `src/test/suite/rustSupport.test.ts` 中编写 Rust 注释扫描测试
  - 测试用例 3: 验证 CLI 扫描 Rust 行注释
  - 测试用例 4: 验证 CLI 扫描 Rust 块注释
  - 测试用例 5: 验证 CLI 扫描 Rust 文档注释(`///`, `//!`)
  - Mock `CliWrapper.scan()` 返回包含 Rust 注释的 JSON
  - 验证 `CommentService.getComments()` 正确解析
  - **预期变更**: 约 60 行

### 用户故事 1 的实施

- [ ] T008 [US1] 修改 `extension.ts` 参数化语言支持逻辑
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/extension.ts`
  - 在 `activate()` 函数开头定义: `const supportedLanguages = ['go', 'rust'];`
  - 替换硬编码检查:
    ```typescript
    // 旧代码
    if (editor.document.languageId !== 'go') {
      console.log(`[CodeI18n] Skipping non-go file: ${editor.document.languageId}`);
      return;
    }
    
    // 新代码
    if (!supportedLanguages.includes(editor.document.languageId)) {
      console.log(`[CodeI18n] Skipping unsupported file: ${editor.document.languageId}`);
      return;
    }
    ```
  - 更新日志消息包含语言信息:
    ```typescript
    console.log(`[CodeI18n] Processing ${editor.document.languageId} file: ${editor.document.fileName}`);
    ```
  - **依赖**: T004 (激活事件已配置)
  - **预期变更**: +5 行,修改 2 行

- [ ] T009 [US1] 为 Rust 注册 Hover Provider
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/extension.ts`
  - 在现有 Go Hover Provider 注册后添加:
    ```typescript
    // Existing: Go Hover Provider
    const goHoverProvider = vscode.languages.registerHoverProvider(
      'go',
      new TranslationHoverProvider(commentService)
    );
    context.subscriptions.push(goHoverProvider);
    
    // New: Rust Hover Provider
    const rustHoverProvider = vscode.languages.registerHoverProvider(
      'rust',
      new TranslationHoverProvider(commentService)
    );
    context.subscriptions.push(rustHoverProvider);
    ```
  - **依赖**: T008 (语言支持已参数化)
  - **预期变更**: +5 行

- [ ] T010 [US1] 运行测试验证用户故事 1 实现
  - 编译代码: `npm run compile`
  - 运行单元测试: `npm test`
  - 验证 T006, T007 的测试通过
  - 如有失败,修复实现代码并重新测试
  - **依赖**: T006-T009 完成

- [ ] T011 [US1] 手动测试 Rust 文件翻译功能
  - 在 VSCode 中按 F5 启动扩展开发宿主
  - 创建测试 Rust 文件:
    ```rust
    // Line comment
    /// Documentation comment
    fn main() {
        /* Block comment */
        println!("Hello");
    }
    ```
  - 验证注释自动翻译显示
  - 验证悬停在注释上显示原文
  - 测试从 Go 文件切换到 Rust 文件,两种语言翻译都正常
  - **依赖**: T010 (单元测试通过)

**检查点**: 用户故事 1 完成 - Rust 文件自动翻译注释功能可独立测试和演示

---

## 阶段 4: 用户故事 2 - Rust注释实时更新翻译(优先级: P2)

**目标**: 用户编辑 Rust 注释后,翻译自动更新,无需手动刷新

**独立测试**: 在 Rust 文件中添加或修改注释,观察翻译在 2 秒内自动更新

**验收标准**:
1. 输入新注释后 2 秒内显示翻译 (500ms debounce + CLI 处理)
2. 修改注释后翻译自动更新
3. 删除注释后装饰立即消失

### 用户故事 2 的测试

- [ ] T012 [P] [US2] 在 `src/test/suite/commentService.test.ts` 中编写缓存逻辑测试
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/test/suite/commentService.test.ts` (新建)
  - 测试用例 1: 验证 Rust 文件缓存命中
    ```typescript
    test('Cache hit for Rust file', async () => {
      const doc = await vscode.workspace.openTextDocument({
        language: 'rust',
        content: '// Test\nfn main() {}'
      });
      
      await commentService.getComments(doc);  // 第一次调用,缓存未命中
      const cached = commentService.getCachedComments(doc);
      
      assert.ok(cached);
      assert.strictEqual(cached.length, mockComments.length);
    });
    ```
  - 测试用例 2: 验证 Go 和 Rust 缓存隔离
  - **预期变更**: 新增文件,约 40 行

- [ ] T013 [P] [US2] 在 `src/test/suite/commentService.test.ts` 中编写错误处理测试
  - 测试用例 3: 验证 CLI 调用失败时优雅降级
  - 测试用例 4: 验证不支持语言返回空数组
  - Mock `CliWrapper.scan()` 抛出错误
  - 验证 `CommentService.getComments()` 返回空数组而非崩溃
  - **预期变更**: 约 30 行

### 用户故事 2 的实施

**注意**: 实时更新功能已由现有的 `onDidChangeTextDocument` 事件和 debounce 机制提供,无需新增代码

- [ ] T014 [US2] 验证现有 debounce 机制支持 Rust 文件
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/extension.ts`
  - 确认 `onDidChangeTextDocument` 事件监听器调用 `update()` 函数
  - 确认 `update()` 函数的语言检查已参数化 (T008 已完成)
  - **依赖**: T008 (参数化语言支持)
  - **预期变更**: 0 行 (验证现有代码即可)

- [ ] T015 [US2] 运行测试验证用户故事 2 实现
  - 运行单元测试: `npm test`
  - 验证 T012, T013 的测试通过
  - **依赖**: T012-T014 完成

- [ ] T016 [US2] 手动测试 Rust 注释实时更新
  - 在扩展开发宿主中打开 Rust 文件
  - 输入新注释: `// New comment`
  - 观察 2 秒内翻译自动显示
  - 修改注释: `// Modified comment`
  - 观察翻译自动更新
  - 删除注释,观察装饰消失
  - **依赖**: T015 (单元测试通过)

**检查点**: 用户故事 2 完成 - Rust 注释实时更新功能可独立测试

---

## 阶段 5: 用户故事 3 - Rust注释悬停提示(优先级: P3)

**目标**: 用户悬停在 Rust 注释上时,显示包含原文和译文的提示框

**独立测试**: 将鼠标悬停在 Rust 注释上,验证提示框显示原文和译文对比

**验收标准**:
1. 悬停在注释上 500ms 内显示提示框
2. 提示框格式清晰,原文和译文明显区分

### 用户故事 3 的测试

- [ ] T017 [P] [US3] 在 `src/test/suite/rustSupport.test.ts` 中编写 Hover Provider 测试
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/test/suite/rustSupport.test.ts`
  - 测试用例 6: 验证悬停在 Rust 行注释上显示提示
    ```typescript
    test('Rust hover provider', async () => {
      const position = new vscode.Position(0, 5);
      const hover = await hoverProvider.provideHover(doc, position, token);
      
      assert.ok(hover);
      assert.ok(hover.contents[0].includes('Test comment'));
    });
    ```
  - 测试用例 7: 验证悬停在 Rust 文档注释上显示提示
  - 测试用例 8: 验证多行注释的范围匹配
  - **预期变更**: 约 40 行

### 用户故事 3 的实施

**注意**: Hover Provider 功能已由 T009 实现,无需新增代码

- [ ] T018 [US3] 验证 TranslationHoverProvider 支持 Rust 文件
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/src/hover/translationHoverProvider.ts`
  - 确认 `provideHover()` 方法使用 `CommentService.getCachedComments()` 获取缓存
  - 确认范围匹配逻辑语言无关 (基于坐标系统)
  - **依赖**: T009 (Rust Hover Provider 已注册)
  - **预期变更**: 0 行 (验证现有代码即可)

- [ ] T019 [US3] 运行测试验证用户故事 3 实现
  - 运行单元测试: `npm test`
  - 验证 T017 的测试通过
  - **依赖**: T017-T018 完成

- [ ] T020 [US3] 手动测试 Rust 注释悬停提示
  - 在扩展开发宿主中打开 Rust 文件
  - 将鼠标悬停在行注释上,验证提示框显示
  - 将鼠标悬停在文档注释上,验证提示框显示
  - 验证提示框包含原文和译文
  - **依赖**: T019 (单元测试通过)

**检查点**: 用户故事 3 完成 - Rust 注释悬停提示功能可独立测试

---

## 阶段 6: 完善与横切关注点

**目的**: 影响所有用户故事的改进和发布准备

- [ ] T021 [P] [Polish] 更新 `README.md` 添加 Rust 支持说明
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/README.md`
  - 在 "支持的语言" 部分添加 Rust 说明:
    ```markdown
    ## 支持的语言
    
    - **Go**: 全功能支持,包括行注释 (`//`)、块注释 (`/* */`)
    - **Rust**: v0.2.0+ 支持,包括:
      - 行注释 (`//`)
      - 块注释 (`/* */`)
      - 外部文档注释 (`///`, `/** */`)
      - 内部文档注释 (`//!`, `/*! */`)
    ```
  - 添加 Rust 使用示例
  - **预期变更**: +20 行

- [ ] T022 [P] [Polish] 更新 `CHANGELOG.md` 记录版本变更
  - 文件路径: `/Users/devinzeng/go/src/github.com/studyzy/codei18n-vscode/CHANGELOG.md` (如果存在)
  - 添加版本记录:
    ```markdown
    ## [0.2.0] - 2025-12-17
    
    ### Added
    - Rust 源码注释翻译支持
      - 支持所有 Rust 注释类型 (行、块、文档注释)
      - 与 Go 语言保持一致的用户体验
      - 独立的 Hover Provider
      - 缓存隔离,支持多语言并行工作
    
    ### Changed
    - 参数化语言支持逻辑,方便未来扩展新语言
    - 改进日志消息,区分不同语言文件
    ```
  - **预期变更**: 新增或修改约 15 行

- [ ] T023 [Polish] 运行完整测试套件并生成覆盖率报告
  - 运行所有测试: `npm test`
  - 生成覆盖率报告: `npm test -- --coverage` (如配置)
  - 验证核心模块覆盖率 > 80%:
    - `extension.ts` (核心逻辑)
    - `commentService.ts` (缓存逻辑)
    - `rustSupport.test.ts` (Rust 特定)
  - 如覆盖率不足,补充测试用例
  - **依赖**: T006-T020 (所有测试编写完成)

- [ ] T024 [Polish] 性能测试: 大型 Rust 文件
  - 使用 quickstart.md 中的 Python 脚本生成大型 Rust 文件:
    ```bash
    python3 specs/002-rust-comment-translation/quickstart.md#generate-large-rust.py
    # 生成包含 1000 条注释的 large_test.rs
    ```
  - 在扩展开发宿主中打开 `large_test.rs`
  - 测量打开文件到翻译显示的时间 (目标: < 3 秒)
  - 测量编辑注释到更新翻译的时间 (目标: < 2 秒)
  - 记录性能指标,如不达标则优化或添加进度指示器
  - **依赖**: T011, T016, T020 (手动测试完成)

- [ ] T025 [Polish] 代码清理和注释补充
  - 为 `src/decoration/decorator.ts` 的坐标转换添加注释:
    ```typescript
    // 坐标系统转换: CLI 输出为 1-indexed,VSCode API 为 0-indexed
    const vscodeStart = new vscode.Position(
      cliRange.startLine - 1,  // 1-indexed → 0-indexed
      cliRange.startCol - 1
    );
    ```
  - 检查所有导出函数是否有 JSDoc 注释
  - 删除未使用的 `src/decoration/decorationTypes.ts` (如确认未使用)
  - **依赖**: 所有实现完成

- [ ] T026 [Polish] 运行 quickstart.md 验证流程
  - 按照 `specs/002-rust-comment-translation/quickstart.md` 的步骤执行完整验证
  - 验证前置条件检查
  - 验证测试运行
  - 验证手动测试场景
  - 确保所有步骤可重现
  - **依赖**: T023-T025 完成

- [ ] T027 [Polish] 更新版本号并准备发布
  - 更新 `package.json` 版本号: `"version": "0.2.0"`
  - 验证 `CHANGELOG.md` 完整 (T022 已完成)
  - 提交所有更改:
    ```bash
    git add .
    git commit -m "feat: Add Rust source code comment translation support"
    ```
  - 创建 Git 标签:
    ```bash
    git tag -a v0.2.0 -m "Release v0.2.0: Add Rust support"
    git push origin 002-rust-comment-translation
    git push origin v0.2.0
    ```
  - **依赖**: T021-T026 完成

- [ ] T028 [Polish] 打包和发布准备
  - 打包扩展: `vsce package` (如已配置)
  - 生成 `.vsix` 文件
  - 在 GitHub 创建 Release,附加 `.vsix` 文件
  - 发布到 VSCode Marketplace (如有权限)
  - **依赖**: T027 完成

**检查点**: 所有用户故事和完善任务完成,功能可发布

---

## 依赖关系与执行顺序

### 阶段依赖关系

- **设置(阶段 1)**: 无依赖关系 - 可立即开始
- **基础(阶段 2)**: 依赖于设置完成 - 阻塞所有用户故事
- **用户故事 1(阶段 3)**: 依赖于基础完成 - MVP 核心功能
- **用户故事 2(阶段 4)**: 依赖于基础完成 - 可与 US1 并行或在 US1 后进行
- **用户故事 3(阶段 5)**: 依赖于基础完成,部分依赖 US1 (Hover Provider 注册)
- **完善(阶段 6)**: 依赖于所有用户故事完成

### 用户故事依赖关系

- **用户故事 1(P1)**: 可在基础(阶段 2)后开始 - 核心功能,无其他故事依赖
  - 核心改动: `package.json` (T004), `extension.ts` (T008, T009)
  - 可独立测试和演示
  
- **用户故事 2(P2)**: 可在基础(阶段 2)后开始 - 复用现有 debounce 机制
  - 无代码改动,仅验证和测试
  - 可与 US1 并行测试,但建议在 US1 后进行以确保基础功能稳定
  
- **用户故事 3(P3)**: 依赖于 US1 的 T009 (Rust Hover Provider 注册)
  - 无额外代码改动,仅验证和测试
  - 必须在 US1 完成后进行

### 每个用户故事内部

- **US1**: 测试 (T006, T007) → 实施 (T008, T009) → 验证 (T010, T011)
- **US2**: 测试 (T012, T013) → 验证 (T014) → 测试通过 (T015) → 手动测试 (T016)
- **US3**: 测试 (T017) → 验证 (T018) → 测试通过 (T019) → 手动测试 (T020)

### 任务依赖图

```
T001, T002, T003 (设置,可并行)
    ↓
T004 (package.json)
    ↓
T005 (JSDoc 注释,可与 T004 并行)
    ↓
T006, T007 (US1 测试,可并行)
    ↓
T008 (extension.ts 参数化)
    ↓
T009 (注册 Rust Hover Provider)
    ↓
T010 (运行 US1 测试)
    ↓
T011 (US1 手动测试)
    ↓ (US1 完成检查点)
T012, T013 (US2 测试,可并行)
    ↓
T014 (验证 debounce)
    ↓
T015 (运行 US2 测试)
    ↓
T016 (US2 手动测试)
    ↓ (US2 完成检查点)
T017 (US3 测试)
    ↓
T018 (验证 Hover Provider)
    ↓
T019 (运行 US3 测试)
    ↓
T020 (US3 手动测试)
    ↓ (US3 完成检查点)
T021, T022 (文档更新,可并行)
    ↓
T023 (覆盖率报告)
    ↓
T024 (性能测试)
    ↓
T025 (代码清理)
    ↓
T026 (quickstart 验证)
    ↓
T027 (版本发布)
    ↓
T028 (打包发布)
```

### 并行机会

- **设置阶段**: T002, T003 可并行 (T001 必须先完成)
- **基础阶段**: T005 可与 T004 并行
- **US1 测试**: T006, T007 可并行
- **US2 测试**: T012, T013 可并行
- **完善阶段**: T021, T022 可并行

**注意**: 由于项目规模较小,大部分任务修改相同文件 (`extension.ts`),实际并行机会有限。建议按阶段顺序执行。

---

## 并行示例: 用户故事 1

```bash
# 阶段 3: 用户故事 1 的并行执行示例

# 步骤 1: 并行编写测试 (T006, T007)
任务 1: "在 rustSupport.test.ts 中编写 Rust 文件激活测试"
任务 2: "在 rustSupport.test.ts 中编写 Rust 注释扫描测试"

# 步骤 2: 顺序实施 (T008 → T009,因为修改同一文件)
任务 3: "修改 extension.ts 参数化语言支持逻辑"
任务 4: "为 Rust 注册 Hover Provider"

# 步骤 3: 验证
任务 5: "运行测试验证用户故事 1 实现"
任务 6: "手动测试 Rust 文件翻译功能"
```

---

## 实施策略

### 仅 MVP(仅用户故事 1)

如果目标是快速交付最小可行产品 (MVP):

1. **完成阶段 1**: 设置 (T001-T003) - 约 15 分钟
2. **完成阶段 2**: 基础 (T004-T005) - 约 20 分钟
3. **完成阶段 3**: 用户故事 1 (T006-T011) - 约 1.5 小时
4. **停止并验证**: 独立测试 Rust 文件翻译功能
5. **部署/演示**: 打包并演示 MVP

**总时间**: 约 2-2.5 小时

**交付价值**: Rust 文件自动翻译注释的核心功能

### 增量交付

如果目标是增量交付所有用户故事:

1. **完成设置 + 基础** (T001-T005) → 基础就绪 - 约 35 分钟
2. **添加用户故事 1** (T006-T011) → 独立测试 → 部署/演示(MVP!) - 约 1.5 小时
3. **添加用户故事 2** (T012-T016) → 独立测试 → 部署/演示 - 约 1 小时
4. **添加用户故事 3** (T017-T020) → 独立测试 → 部署/演示 - 约 45 分钟
5. **完善与发布** (T021-T028) → 最终发布 - 约 1.5 小时

**总时间**: 约 5-6 小时

**交付价值**: 完整的 Rust 注释翻译功能,包括实时更新和悬停提示

### 并行团队策略

如果有多个开发人员:

1. **团队一起完成设置 + 基础** (T001-T005) - 约 35 分钟
2. **基础完成后并行分工**:
   - **开发人员 A**: 用户故事 1 (T006-T011)
   - **开发人员 B**: 用户故事 2 测试编写 (T012-T013,可提前准备)
   - **开发人员 C**: 文档更新 (T021-T022,可提前准备)
3. **顺序集成**:
   - US1 完成 → US2 验证 (T014-T016)
   - US2 完成 → US3 测试和验证 (T017-T020)
4. **最终完善**: 团队协作完成 T023-T028

**总时间**: 约 3-4 小时 (团队协作)

---

## 估算总结

| 阶段 | 任务数 | 预计时间 | 关键路径 |
|------|--------|---------|---------|
| 设置 | 3 | 15 分钟 | T001 必须先完成 |
| 基础 | 2 | 20 分钟 | T004 阻塞所有故事 |
| US1 (P1) | 6 | 1.5 小时 | MVP 核心 |
| US2 (P2) | 5 | 1 小时 | 增强功能 |
| US3 (P3) | 4 | 45 分钟 | 增强功能 |
| 完善 | 8 | 1.5 小时 | 发布准备 |
| **总计** | **28** | **5-6 小时** | **顺序执行** |

**并行潜力**: 约 20% (设置和完善阶段部分任务可并行)

**MVP 时间**: 约 2-2.5 小时 (仅 US1)

---

## 注意事项

- **[P] 任务**: 标记为 [P] 的任务可并行执行 (不同文件,无依赖关系)
- **[Story] 标签**: 将任务映射到特定用户故事以实现可追溯性
- **独立测试**: 每个用户故事应该独立可完成和可测试
- **TDD 顺序**: 在实施前验证测试失败,实施后验证测试通过
- **提交频率**: 在每个任务或逻辑组后提交 (建议每完成 2-3 个任务提交一次)
- **检查点停止**: 在每个用户故事完成后停止,独立验证故事功能
- **避免事项**: 
  - 模糊任务 (所有任务都有明确的文件路径和预期变更)
  - 相同文件冲突 (大部分任务修改 `extension.ts`,必须顺序执行)
  - 破坏独立性的跨故事依赖 (US2, US3 基本独立于 US1)

---

## 快速参考

### 必须先完成的任务 (阻塞任务)

- **T001**: CLI 工具验证 - 如失败则暂停整个功能
- **T004**: package.json 激活事件 - 阻塞所有用户故事

### 可跳过的任务 (可选优化)

- **T005**: JSDoc 注释 - 改进文档但不影响功能
- **T022**: CHANGELOG 更新 - 发布时需要但开发时可跳过
- **T025**: 代码清理 - 优化但不影响功能

### 最小 MVP 路径

```
T001 → T002 → T003 → T004 → T006 → T007 → T008 → T009 → T010 → T011
(设置)  (设置)  (设置)  (基础)  (US1)  (US1)  (US1)  (US1)  (US1)  (US1)
```

**10 个任务,约 2-2.5 小时**

---

**任务分解完成日期**: 2025-12-17
**下一步**: 运行 `/speckit.implement` 开始实施,或按 MVP 路径手动执行任务
